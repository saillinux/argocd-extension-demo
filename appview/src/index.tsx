import * as React from 'react';

export const Extension = (props: {
  tree: any;
  application: any;
}) => (
  <div>Hello AppView Demo!</div>
);

export const component = Extension;
export const title = "AppView Demo";
export const icon = "fa-th";
